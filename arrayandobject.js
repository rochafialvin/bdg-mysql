// ARRAY
let hari = ['Senin', 'Selasa', 'Rabu']
// index = posisi data, dimulai dari 0
// index 0 = 'Senin', dan index 2 = 'Rabu'
hari[0] // Senin
// function dari array : map, filter, foreach

/*
    case untuk array
    1. Ketika ingin memberikan se-deret data ke dalam function (foreach)
    2. Ingin menfilter se-deret data dengan sebuah function (filter) 
*/

// OBJECT
let user = {
    name: 'Maxx Corner',
    age: 39,
    job: 'Freelancer'
}
// property = nama dari setiap data yang disimpan
user.name
// function = keys, values.

/*
    case untuk object
    1. Ketika ingin mengirim data ke API, API hanya menerima pada dalam bentuk object
    2. Ingin menyimpan data ke file JSON, kode untuk menyimpan data ke JSON hanya menerima object
*/